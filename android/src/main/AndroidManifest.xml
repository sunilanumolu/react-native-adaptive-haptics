<manifest xmlns:android="http://schemas.android.com/apk/res/android">

  <!-- Required for vibration on all Android versions.
       Users must still request this permission at runtime on Android 13+ (API 33). -->
  <uses-permission android:name="android.permission.VIBRATE" />

</manifest>
